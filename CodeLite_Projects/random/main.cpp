#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

const int HIGHEST_NUMBER = 25;

int main() {
    
    
    
}

/*
int main() {
    char response;
    int numberToBeGuessed, playersGuess;
    srand(time(0));
    do {
        numberToBeGuessed = rand() % (HIGHEST_NUMBER + 1);
        cout << "guess a number between 0 and " << HIGHEST_NUMBER << ":";
        cin >> playersGuess;
        while (playersGuess != numberToBeGuessed) {
            cout << "Guess again: ";
            cin >> playersGuess;
        }
        cout << "You guessed it. You're amazing!" << endl;
        cout << "Would you like to play again? ";
        cin >> response;
    }while(response == 'y');
}
*/